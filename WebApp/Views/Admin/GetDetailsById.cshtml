@model UserResponse

<table class="table table-hover" style="margin-top:300px">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>PhoneNo</th>
        </tr>

    </thead>
    <tr>
        <td>@Model.UserName</td>
        <td>@Model.Email</td>
        <td>@Model.PhoneNo</td>
    </tr>
</table>